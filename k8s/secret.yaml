# Values are encoded in base64 — it's not encryption, just a way to safely store them in YAML.

apiVersion: v1
kind: Secret
metadata:
  name: demo-devops-secret
  namespace: demo-devops
type: Opaque
data:
  # Django's secret key — it uses it to sign sessions and tokens.
  DJANGO_SECRET_KEY: ZGphbmdvLWluc2VjdXJlLXRlc3Qta2V5LWNoYW5nZS1pbi1wcm9kdWN0aW9u
